name: educoresystem
description: EduCore System
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core:
  firebase_auth:
  cloud_firestore:
  provider:
  intl:
  google_fonts:
  fl_chart:            # reports ke liye (optional)
  pdf:                  # PDF reports ke liye (optional)
  printing:             # PDF print/share ke liye (optional)
  path_provider:         # PDF save karne ke liye (optional)
  image_picker:
  flutter_image_compress:
  image_picker_web:
  image:
  shared_preferences:
  shimmer:
  open_file:
  universal_platform:
  share_plus:







dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0


flutter_launcher_icons:
flutter_native_splash:


  flutter_launcher_icons:
    android: true
    ios: true
    image_path: "assets/images/EduCoreSystem.png"
    adaptive_icon_background: "#FFFFFF"
    adaptive_icon_foreground: "assets/images/EduCoreSystem.png"


  flutter_native_splash:
    color: "#FFFFFF"
    image: "assets/images/EduCoreSystem.png"
    android: true
    ios: true
    web: true


flutter:
  uses-material-design: true
  # assets add kar sakte ho agar koi image chahiye
  assets:
    - assets/images/
    - assets/images/Logo.png
    - assets/images/EduCoreSystem.png
